import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { HeroSection } from "@/components/home/HeroSection"; import { ProductTile } from "@/components/home/ProductTile"; import { QuoteSection } from "@/components/home/QuoteSection"; import { JsonLd } from "@/components/seo/JsonLd"; import { getSiteContent } from "@/content/site-content"; import { isValidLocale, type Locale } from "@/i18n/config"; import { absoluteUrl, siteConfig } from "@/lib/site";
type P={params:Promise<{locale:string}>}; export async function generateMetadata({params}:P):Promise<Metadata>{const {locale}=await params;if(!isValidLocale(locale))notFound();const c=getSiteContent(locale);return{title:c.metadata.title,description:c.metadata.description,alternates:{canonical:absoluteUrl(`/${locale}`)},openGraph:{title:c.metadata.title,description:c.metadata.description,url:absoluteUrl(`/${locale}`),images:[{url:absoluteUrl(siteConfig.defaultOgImage),width:1200,height:630}]}}}
export default async function HomePage({params}:P){const {locale}=await params;if(!isValidLocale(locale))notFound();const c=getSiteContent(locale as Locale);return <><JsonLd data={{"@context":"https://schema.org","@type":"Person",name:c.identity.name,jobTitle:c.identity.role,email:c.identity.email,address:c.identity.location,url:absoluteUrl(`/${locale}`)}}/><HeroSection hero={c.home.hero}/><section id="work" aria-label="Selected work">{c.home.productTiles.map(t=><ProductTile key={t.id} tile={t}/>)}</section><QuoteSection quote={c.home.quote}/></>}
